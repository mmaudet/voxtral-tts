{
  "voice-factory-main": {
    "podId": "<assigned-on-create>",
    "name": "voice-factory-main",
    "gpuTypeId": "NVIDIA RTX A5000",
    "gpuCount": 1,
    "imageName": "runpod/pytorch:2.4.0-py3.11-cuda12.4.1-devel-ubuntu22.04",
    "containerDiskInGb": 50,
    "volumeInGb": 50,
    "volumeMountPath": "/workspace",
    "ports": ["8000/http", "4000/http", "22/tcp"],
    "publicIp": "<assigned>",
    "sshPort": "<assigned-22nnnn>",
    "costPerHrRunning": 0.27,
    "cloudType": "SECURE",
    "envKeys": ["HF_HOME", "HF_TOKEN", "PUBLIC_KEY"],
    "publicEndpoints": {
      "litellm": "https://<podId>-4000.proxy.runpod.net/v1/audio/speech"
    },
    "note": "Only LiteLLM (4000) is externally reachable. vLLM (8000) binds to 127.0.0.1 inside the pod, so the public 8000 URL returns 502. Endpoints are only live while currentStatus == RUNNING; restart with POST /v1/pods/<id>/start."
  }
}
